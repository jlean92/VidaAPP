<Window
    x:Class="VidaApp.WinUI.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="VidaApp - Skeleton"
    Width="900"
    Height="600">

    <Grid Padding="16" RowSpacing="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Vertical" Spacing="8">
            <TextBlock Text="Base URL" FontWeight="SemiBold"/>
            <TextBox x:Name="BaseUrlTextBox"
                     Text="https://jlean92.es"
                     PlaceholderText="https://jlean92.es"
                     Width="420"/>
            <TextBlock x:Name="StatusTextBlock" Text="" Foreground="Gray"/>
        </StackPanel>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Spacing="12">
            <Button Content="Ping /health" Click="OnPingHealthClick"/>
            <Button Content="Ping /health/db" Click="OnPingDbHealthClick"/>
            <Button Content="Limpiar" Click="OnClearClick"/>
        </StackPanel>

        <TextBox Grid.Row="2"
                 x:Name="OutputTextBox"
                 FontFamily="Consolas"
                 IsReadOnly="True"
                 AcceptsReturn="True"
                 TextWrapping="NoWrap"
                 VerticalScrollBarVisibility="Auto"
                 HorizontalScrollBarVisibility="Auto"/>
    </Grid>
</Window>
